{
  "name": "@propgate/cli",
  "version": "0.0.0",
  "description": "Diagnose DNS configuration from the terminal, using the same engine as propgate.dev.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/joaopcm/propgate",
    "directory": "packages/cli"
  },
  "bin": {
    "propgate": "./dist/index.js"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "bin": {
      "propgate": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@propgate/dns": "workspace:*"
  },
  "devDependencies": {
    "@propgate/dns-fixtures": "workspace:*",
    "@types/node": "^24.13.3",
    "tsup": "^8.5.1",
    "typescript": "^5.7",
    "vitest": "^4"
  }
}
