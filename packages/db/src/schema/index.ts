// biome-ignore-all lint/performance/noBarrelFile: drizzle resolves the schema through one namespace

// Empty until the first table lands. Drizzle takes the schema as a namespace
// import, so the module has to exist and be valid before there is anything in
// it to export.
export {};
