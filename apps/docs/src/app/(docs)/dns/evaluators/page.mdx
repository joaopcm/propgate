import { CodeBlock } from "@/components/docs/code-block";
import { REQUIREMENT_TYPES } from "@/lib/api";
import { EVALUATOR_ENTRIES } from "./_entries";
import { CHECK_KINDS } from "@propgate/dns";
import { RUN_CHECKS_BASIC } from "./_snippets";

# The evaluators

Every evaluator is listed below, rendered from the package itself. `runChecks`
is what a caller actually reaches for: it takes a `DomainProfile` (what a domain
is *for*, stated as data) and runs whichever checks that profile asks for
against one `EvaluationContext`.

<CodeBlock code={RUN_CHECKS_BASIC} lang="ts" />

Two decisions live in `runChecks` that no single evaluator could make on its
own:

- **Checks run concurrently**, each against its own context. They share
  nothing and none is ordered relative to another, so the wall clock is the
  slowest check rather than the sum of them all.
- **A skipped check is not a passing check.** A profile that never asks about
  DKIM produces no DKIM outcome at all, rather than a green one. `sendingOnly`,
  `fullMail`, and `webOnly` are the three profile constructors that ship with
  the package; a `DomainProfile` is otherwise plain data you can construct
  and store yourself.

The verdict on the result is the worst of its parts, and `indeterminate`
ranks above `warn`: one check that could not run makes the whole answer
uncertain, but a definite failure you did observe is more actionable than
uncertainty about the rest.

## The checks

Rendered from `REQUIREMENT_TYPES`, the same record the API reference reads, so
this list cannot say something the resolver cannot actually check.

<div>
  {CHECK_KINDS.map((kind) => {
    const type = REQUIREMENT_TYPES[kind];
    const entry = EVALUATOR_ENTRIES[kind];

    return (
      <section className="my-8 border-border border-t pt-6" key={kind}>
        <h3 className="mb-2 font-mono text-base text-foreground">{kind}</h3>
        <p className="mb-3 text-muted-foreground text-sm leading-6">
          {type.summary}
        </p>
        <p className="mb-3 text-sm leading-6">
          <span className="text-foreground">The non-obvious part: </span>
          <span className="text-muted-foreground">{entry.nonObvious}</span>
        </p>
        <CodeBlock code={entry.code} lang="ts" />
      </section>
    );
  })}
</div>

## Next

[Recipes](/dns/recipes) puts this together into three complete files: check a
domain and switch on the verdict, query a specific resolver on a
non-standard port, and read the lookups behind a finding well enough to
explain it to a customer.
