import { Callout } from "@/components/docs/callout";
import { CodeBlock } from "@/components/docs/code-block";
import { EndpointHeader } from "@/components/docs/endpoint-header";
import { VERIFY_CURL, VERIFY_RATE_LIMITED, VERIFY_RESPONSE } from "./_snippets";

# Verify a domain

<EndpointHeader method="POST" path="/v1/domains/:id/checks" />

Runs every check the domain's profile names, updates its state, and returns a
result per requirement. This is the only way a domain moves out of `pending`
— **continuous re-checking is the sweeper's job and does not come through
here.** A customer clicking "verify" during onboarding is what this endpoint
is for; reconciling ten thousand domains on a schedule is not, and is not
rate-limited the same way either.

No CLI command yet — triggering a check is API-only.

<Callout>
  Both the request and the response below are quoted verbatim from
  QUICKSTART.md, a real run against the live API. QUICKSTART.md trims the
  `requirements` array to two of the five entries the registered profile
  actually has, which is why `requirementsTotal: 5` but only two are shown.
</Callout>

<CodeBlock code={VERIFY_CURL} lang="bash" />
<CodeBlock code={VERIFY_RESPONSE} lang="json" />

Every finding carries a [diagnosis code](/taxonomy) and, where the evaluator
has them, the DNS `name` it concerns, what was `observed`, and what was
`expected`. Findings can appear on a satisfied requirement too — read
`satisfied`, not the presence of findings, the way `mx` above would still show
`MX_NULL` at `pass` for a domain that correctly declares it sends no mail.

If the check causes a state transition — `pending` to `verified`, `verified`
to `failed`, and so on — the same webhook fires as when the sweeper causes
one. See [Webhooks](/webhooks) for the event names and payload shape.

## Rate limit

**100 checks a minute per account**, separate from the general request limit
of 250 requests a second. A check costs up to about 20 upstream DNS queries,
aimed at whichever authoritative servers the *caller* names — not servers we
operate — which is why this is the limit sized as a tripwire rather than a
quota: it protects other people's infrastructure, not ours.

<Callout>
  The 429 below is quoted from the error the route itself constructs
  (`apps/api/src/routes/domains.ts`), not a captured response.
</Callout>

<CodeBlock code={VERIFY_RATE_LIMITED} lang="http" />

A `Retry-After` header names how long to wait. If a real integration reaches
this limit, tell us — it is a number we would rather re-measure than have you
build around.
