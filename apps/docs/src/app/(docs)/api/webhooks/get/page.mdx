import { CodeBlock } from "@/components/docs/code-block";
import { CodeTabs } from "@/components/docs/code-tabs";
import { EndpointHeader } from "@/components/docs/endpoint-header";
import { GET_CURL, GET_NOT_FOUND, GET_RESPONSE, GET_SDK } from "./_snippets";

# Get a webhook endpoint

<EndpointHeader method="GET" path="/v1/webhooks/:id" />

One endpoint's configuration: its URL, which events it subscribes to, and
whether it is currently disabled.

<CodeTabs
  items={[
    { code: GET_CURL, label: "cURL", lang: "bash" },
    { code: GET_SDK, label: "SDK", lang: "ts" },
  ]}
/>

<CodeBlock code={GET_RESPONSE} lang="json" />

An id that does not exist, or belongs to a different account, is a `404` —
the two are deliberately indistinguishable, so this endpoint cannot be used
to discover whether an id exists on someone else's account:

<CodeBlock code={GET_NOT_FOUND} lang="json" />
