import { CodeBlock } from "@/components/docs/code-block";
import { CodeTabs } from "@/components/docs/code-tabs";
import { EndpointHeader } from "@/components/docs/endpoint-header";
import { DELETE_CURL, DELETE_RESPONSE, DELETE_SDK } from "./_snippets";

# Delete a webhook endpoint

<EndpointHeader method="DELETE" path="/v1/webhooks/:id" />

Removes the endpoint. Nothing is sent to it again, and its secret stops being
valid. To pause an endpoint you intend to bring back, use
[`disabled`](/api/webhooks/update) instead — there is no undo here.

<CodeTabs
  items={[
    { code: DELETE_CURL, label: "cURL", lang: "bash" },
    { code: DELETE_SDK, label: "SDK", lang: "ts" },
  ]}
/>

<CodeBlock code={DELETE_RESPONSE} lang="json" />

Deleting an endpoint does not delete its past deliveries. "Why did this
webhook never arrive" is a question that outlives the endpoint it was asked
about, so the [delivery records](/api/webhooks/deliveries) stay.
